/**
 * Rabbit Ear (c) Kraft
 */
/**
 * @param {object} a FOLD graph
 * @param {number[]} two face indices in an array.
 */
// const join_faces = (graph, faces) => {
// 	const face_0_pairs = {};
// 	graph.faces_vertices[faces[0]]
// 		.map((v, i, arr) => [v, arr[(i + 1) % arr.length]])
// 		.forEach((pair, i) => { face_0_pairs[pair] = i; });
// 	const matches = [];
// 	graph.faces_vertices[faces[1]]
// 		.map((v, i, arr) => [v, arr[(i + 1) % arr.length]])
// 		.forEach()
// };

// export default join_faces;
