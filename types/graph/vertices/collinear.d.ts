export function getOtherVerticesInEdges({ edges_vertices }: FOLD, vertex: number, edges: number[]): number[];
export function isVertexCollinear({ vertices_coords, vertices_edges, edges_vertices, }: FOLD, vertex: number, epsilon?: number): boolean;
